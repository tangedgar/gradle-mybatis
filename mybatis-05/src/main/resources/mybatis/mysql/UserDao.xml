<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tanghui05.dao.UserDao">
    <select id="getUserList" resultType="com.tanghui05.model.User">
        select * from user;
    </select>
    <select id="getUserById" parameterType="int" resultType="com.tanghui05.model.User">
        select * from user where id = #{id}
    </select>
    <update id="updateUser" parameterType="com.tanghui05.model.User">
        update user set name=#{name},pwd=#{pwd} where id=#{id}
    </update>
</mapper>